# Seed Scripts

Run:

```bash
npm run db:seed
```

The seed creates:

- Admin user: `admin@elegantbeauty.local`
- Default door-to-door salon services for African plaiting, hairstyles, nails, skincare, children hair care, event preparation, and interview grooming

Default admin password:

```text
AdminPass123!
```

Change the admin password immediately after production deployment.
